{
  "name": "@firewatch/fire-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@firewatch/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8",
    "vitest": "^3"
  }
}
